
      <article class="mb-24">
          <a class="w-100" href="{{ token.url }}">
            <div class="rat-1-1 w-100">
              <div class="rat-content">
                <img class="video-cover" width="100%" height="100%" src="{{ token.image }}" alt="{{ token.data.imageAlt }}"
                  class="h1">
              </div>
            </div>
                </a>
          <div class="article-text mt-24">
              <h3 class="fluid-8-b"><a href="{{ token.fecha }}">{{ token.date }}</a></h3>
              <p class="mb-8 p1 c-dark-grey">{{ token.tags | postDate }}</span></p>
              {% for tag in token.data.tags %}
              <div class=" tag-info ">
                {{ tag }}
              </div>
              {% endfor %}
              <p class="mb-24">{{ token.data.description }}</p>
              <a href="{{ token.url }}" class="btn link-line"> Leer más</a>
          </div>
      </article>
